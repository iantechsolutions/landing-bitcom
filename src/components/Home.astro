---
import { ChevronRight } from "lucide-astro";
---

<section
  class="w-full flex flex-col md:flex-row justify-center gap-3 items-center md:mt-20 px-20 2xl:max-w-7xl 2xl:mx-auto"
  id="home"
>
  <div class="header-home">
    <h1>
      Simplifique la gestión de tesorería
      <span>de su empresa</span>
    </h1>
    <p>
      Acceda a un servicio integral de gestión de canales de recaudación y cash
      management de forma online con nuestra plataforma.
    </p>

    <button>
      Accedé a nuestro sistema <ChevronRight
        class="h-4"
        strokeWidth={1}
      /></button
    >
  </div>
  <div>
    <img src="/images/mockup 1.png" class="40vw" alt="" />
    <div></div>

    <style>
      .home {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      .header-home h1 {
        font-size: 2rem;
        font-size: clamp(
          2rem,
          1.7628865979381443rem + 1.0309278350515463vw,
          3rem
        );
        font-weight: 600;
        text-align: start;
        margin-top: 8rem;
      }

      .header-home h1 span {
        color: #85ce81;
      }

      .header-home p {
        font-size: 0.8rem;
        font-size: clamp(
          0.8rem,
          0.6340206185567011rem + 0.7216494845360825vw,
          1.5rem
        );
        font-weight: 400;
        text-align: start;
        margin-top: 1.5rem;
      }

      .header-home button {
        display: flex;
        justify-content: space-between;
        align-items: center;
        width: 25rem;
        border: none;
        background-color: #bef0bb;
        color: #366b33;
        font-weight: 700;
        font-size: 1rem;
        padding: 0.9rem 1.2rem;
        margin-top: 2rem;
        border-radius: 9999px;
      }

      @media (max-width: 1024px) {
        .header-home button {
          width: 15rem;
          font-size: 0.8rem;
        }
      }
    </style>
  </div>
</section>
